<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="title"
            type="String" />
        <variable
            name="description"
            type="String" />
        <variable
            name="thumbnail"
            type="android.widget.ImageView" />
        <variable
            name="itemClicklistener"
            type="com.example.attractions.view.AttractionListAdapter.OnItemClicklistener" />
        <variable
            name="data"
            type="com.example.attractions.network.model.Data" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/item_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:onClick="@{() -> itemClicklistener.onItemClick(data)}"
        android:tag="attraction_list_root_tag">

        <ImageView
            android:id="@+id/iv_thumb"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="96dp"
            android:minHeight="96dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_margin="5dp"
            android:src="@android:drawable/star_on" />

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@+id/iv_thumb"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/iv_right"
            tools:text="title title title title title title title title title title"
            android:ellipsize="end"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_margin="5dp"
            android:maxLines="2" />

        <TextView
            android:id="@+id/tv_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@+id/tv_title"
            app:layout_constraintStart_toEndOf="@+id/iv_thumb"
            app:layout_constraintEnd_toStartOf="@+id/iv_right"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="description description description description description description "
            android:ellipsize="end"
            android:textSize="16sp"
            android:layout_margin="5dp"
            android:maxLines="2" />

        <ImageView
            android:id="@+id/iv_right"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_margin="5dp"
            android:src="@drawable/icon_right_arrow" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>